# edge-detection-tracking-jetson-notes

> **Status: work in progress.** Scaffolded, not yet implemented. This README
> will carry generated numbers once the first results land.

YOLO detect-then-track on public video, ONNX export, INT8 calibration, mAP-vs-latency measured, with camera-calibration and sensor-fusion notes.

**This repo answers one question:**

> What does INT8 quantization cost in mAP, and what could not be verified without a Jetson?

Everything here serves answering that. Features that do not help answer it are
out of scope — deliberately.

> **Scope constraint.** 30h HARD CAP. B4 is 5% of the inventory and explicitly must not grow. Do not feature on the site front page.

## Why this exists

<!-- 2-4 sentences: what was hard, what this establishes. Written last. -->

## Quickstart

```bash
uv sync --extra dev
uv run pytest -q
uv run python scripts/generate_results.py
```

## Results

<!-- Generated by scripts/generate_results.py into results/. Every number
     carries: date, hardware, model snapshot, seed, reproduce command, and
     the raw artifact path. No number is typed by hand. -->

_No results yet._

## Design decisions

<!-- The judgement artifact for this repo:
     ADR: ONNX INT8 and what I could not verify. TensorRT engine-build script shipped UNEXECUTED with an explicit no-Jetson-available statement. SLAM and sensor fusion as written notes, not fake code.
-->

## Limitations

<!-- At least one thing this repo does NOT establish. Written honestly. -->

## Concepts covered

- 4 YOLO family, anchor-based vs anchor-free, NMS, IoU, mAP
- 4 detection, segmentation, tracking (ByteTrack association)
- 4 camera geometry: intrinsics, extrinsics, projection, calibration
- 4 sensor fusion: early vs late, time synchronization, coordinate frames
- 4 SLAM: front-end odometry, pose-graph back-end, loop closure, GPS-denied failure modes
- 4 TensorRT: layer fusion, INT8 calibration, engine building
- …and 2 more (see `docs/inventory-coverage.md`)

## License

MIT
